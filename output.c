#include <stdio.h>
int main()
{
    // int rahim, karim;
    // char c;
    // float f;
    // scanf("%d %d %f %c", &rahim, &karim, &f, &c);
    // printf("%d %d %0.2f %c", rahim, karim, f, c);

    // int a, b;
    // char p;
    // scanf("%d%c %d%c", &a, &p, &b, &p);
    // printf("%d%% %d%%", a, b);

    // int a, b;
    // scanf("%d%% %d%%", &a, &b);
    // printf("%d%% %d%%", a, b);

    int a, b;
    scanf("%da %db", &a, &b);
    printf("%da %db", a, b);
    return 0;
}