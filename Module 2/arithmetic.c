#include <stdio.h>
int main()
{
    // int a = 10, b = 20;

    // int sum = a + b;
    // printf("%d", sum);

    // int minus = a - b;
    // printf("%d", minus);

    // int multiply = a * b;
    // printf("%d", multiply);

    // int divide = b / a;
    // printf("%d", divide);

    int a = 3, b = 20;

    // float divide = b * 1.0 / a;
    // printf("%f", divide);

    int mod = b % a;
    printf("%d", mod);
    return 0;
}